# Gemini Adapter

Configure Gemini CLI to load Good Partner rules via a context file.

## Install
1) Copy `settings.json` to `.gemini/settings.json` in your repo.
2) Copy `GOODPARTNER.md` to repo root.
3) Ensure good-partner/PROTOCOL.md exists.
